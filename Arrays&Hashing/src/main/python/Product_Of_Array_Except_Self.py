class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        res, prod_vals = [], []
        curr_index = 0
        
        for num in enumerate(nums):
            if curr_index = 
            prod_vals.append(num)

                
        